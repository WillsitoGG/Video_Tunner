name: Phase 2E Case 298 Thread Repro Once

on:
  push:
    branches:
      - phase2e-post-render-verification
    paths:
      - .github/workflows/phase2e-case298-thread-repro-once.yml

concurrency:
  group: video-tunner-phase2e-case298-thread-repro-${{ github.ref }}
  cancel-in-progress: true

env:
  PHASE2E_HUMAN_FIXTURE: tests\fixtures\phase2e_human_render_closeout_ami_v1.json
  PHASE2E_FFMPEG_URL: https://github.com/BtbN/FFmpeg-Builds/releases/download/autobuild-2026-09-05-13-10/ffmpeg-n9.0.1-26-g5c8e7e2433-win64-gpl-9.0.zip
  PHASE2E_EXPECTED_FFMPEG_SHA256: a8ebbaf7a99185f5abc3a2d3a657521c38d7966f06b70468d7ab29a67fe8654f
  PHASE2E_EXPECTED_FFMPEG_VERSION_TOKEN: n9.0.1-26-g5c8e7e2433

jobs:
  windows-case298-thread-repro:
    runs-on: windows-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Preflight diagnostic
        shell: pwsh
        run: |
          python -m py_compile .\.github\scripts\run_phase2e_case298_thread_repro_diagnostic.py .\Source\video_tunner\transcription_consensus.py
          Write-Host "PHASE2E_CASE298_THREAD_REPRO_PREFLIGHT=PASS"

      - name: Install pinned analysis runtime
        run: python -m pip install -e .[analysis]

      - name: Acquire immutable FFmpeg build
        shell: pwsh
        run: |
          $root = Join-Path $env:RUNNER_TEMP "Video Tunner Phase2E Case298 Thread Repro FFmpeg"
          $zip = Join-Path $root "ffmpeg.zip"
          $extract = Join-Path $root "extract"
          New-Item -ItemType Directory -Force -Path $root | Out-Null
          Invoke-WebRequest -Uri $env:PHASE2E_FFMPEG_URL -OutFile $zip
          $sha = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          Write-Host "PHASE2E_CASE298_THREAD_REPRO_FFMPEG_SHA256=$sha"
          if ($sha -ne $env:PHASE2E_EXPECTED_FFMPEG_SHA256) { throw "FFmpeg archive SHA mismatch: $sha" }
          Expand-Archive -Path $zip -DestinationPath $extract -Force
          $ffmpeg = Get-ChildItem $extract -Filter ffmpeg.exe -Recurse | Select-Object -First 1
          $ffprobe = Get-ChildItem $extract -Filter ffprobe.exe -Recurse | Select-Object -First 1
          if (-not $ffmpeg -or -not $ffprobe) { throw "Immutable FFmpeg archive missing tools" }
          $versionLine = (& $ffmpeg.FullName -version | Select-Object -First 1 | Out-String).Trim()
          Write-Host "PHASE2E_CASE298_THREAD_REPRO_FFMPEG_VERSION=$versionLine"
          if ($versionLine -notlike "*$($env:PHASE2E_EXPECTED_FFMPEG_VERSION_TOKEN)*") {
            throw "Unexpected FFmpeg build: $versionLine"
          }
          "PHASE2E_DIAGNOSTIC_FFMPEG=$($ffmpeg.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append
          "VIDEO_TUNNER_FFMPEG_DIR=$($ffmpeg.DirectoryName)" | Out-File -FilePath $env:GITHUB_ENV -Append
          "PHASE2E_DIAGNOSTIC_FFMPEG_SHA256=$sha" | Out-File -FilePath $env:GITHUB_ENV -Append
          "PHASE2E_DIAGNOSTIC_FFMPEG_VERSION_LINE=$versionLine" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Download licensed AMI speaker-specific sources
        shell: pwsh
        run: .\.github\scripts\download_ami_positive_closeout_fixtures.ps1 -Fixture $env:PHASE2E_HUMAN_FIXTURE

      - name: Acquire pinned large-v3-turbo model
        shell: pwsh
        run: .\.github\scripts\download_large_v3_turbo_direct.ps1

      - name: Compare runtime-default vs serial CPU repeatability
        shell: pwsh
        run: |
          $env:PYTHONPATH = "$PWD\Source"
          $env:HF_HUB_OFFLINE = "1"
          $env:HF_HUB_DISABLE_XET = "1"
          $output = Join-Path $env:RUNNER_TEMP "Video Tunner Phase2E Case298 Thread Repro"
          python .\.github\scripts\run_phase2e_case298_thread_repro_diagnostic.py `
            --fixture $env:PHASE2E_HUMAN_FIXTURE `
            --model-stage $env:SPANISH_TARGET_MODEL_STAGE `
            --ffmpeg $env:PHASE2E_DIAGNOSTIC_FFMPEG `
            --ffmpeg-archive-sha256 $env:PHASE2E_DIAGNOSTIC_FFMPEG_SHA256 `
            --ffmpeg-version-line $env:PHASE2E_DIAGNOSTIC_FFMPEG_VERSION_LINE `
            --output $output

      - name: Upload lightweight diagnostic manifest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2e-case298-thread-repro
          path: ${{ runner.temp }}\Video Tunner Phase2E Case298 Thread Repro\manifest.json
          if-no-files-found: warn
          retention-days: 7
          compression-level: 9

      - name: Scope note
        if: always()
        shell: pwsh
        run: |
          Write-Host "Diagnostic only: same case 298 master, three fresh-model runs per CPU-thread arm."
          Write-Host "Arm A uses faster-whisper runtime default cpu_threads=0; Arm B fixes cpu_threads=1; both use num_workers=1."
          Write-Host "FFmpeg uses immutable release tag autobuild-2026-09-05-13-10 and verifies both archive SHA and contained build token."
          Write-Host "Prior mutable /latest/ archive SHA is not asserted equal to the immutable asset SHA."
          Write-Host "No product default, timing tolerance, detector, guard, approval, edit, authorization or render is changed."
